name = "dota2-cheats"
main = "worker.js"
compatibility_date = "2025-08-01"

[assets]
directory = "."
binding = "ASSETS"
# Run Worker first so www / HTTP canonical redirects apply before static files.
run_worker_first = true

[build]
command = "npm run build"
